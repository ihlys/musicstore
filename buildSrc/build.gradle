apply plugin: 'groovy'

repositories {
    mavenCentral()
}

ext {
    snakeYamlVersion = '1.18'
}

dependencies {
    compile localGroovy()
    compile gradleApi()

    compile ("org.yaml:snakeyaml:$snakeYamlVersion")
}